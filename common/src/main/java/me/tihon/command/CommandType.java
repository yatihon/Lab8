package me.tihon.command;

public enum CommandType {
    ADD,
    SHOW,
    CLEAR,
    INFO,
    HELP,
    REMOVE_BY_ID,
    UPDATE,
    PRINT_DESCENDING,
    FILTER_STARTS_WITH_NAME,
    FILTER_LESS_THAN_MINUTES,
    ADD_IF_MAX,
    ADD_IF_MIN,
    REMOVE_LOWER,
    LOGIN,
    REGISTER
}

